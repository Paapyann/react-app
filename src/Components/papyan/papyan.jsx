import { Link } from "react-router-dom";

const papyan = () => {
  return (
    <div>
      <Link to="/about">
        <button>Go back</button>
      </Link>
    </div>
  );
};

export default papyan;
